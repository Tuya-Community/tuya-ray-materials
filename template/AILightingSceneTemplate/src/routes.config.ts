import { Routes } from '@ray-js/types';

export const routes: Routes = [
  {
    route: '/auto-generate',
    path: '/pages/auto-generate/index',
  },
  {
    route: '/home',
    path: '/pages/home/index',
  },
];
