import { Todo, Ptz, Interactive, RemoteControl, PathManager } from '@/components/featureComponents';
// 组件映射
export const componentMap = {
  ptz: Ptz,
  interactive: Interactive,
  remoteControl: RemoteControl,
  pathManager: PathManager,
  todo: Todo,
};
