import { Routes } from '@ray-js/types';

export const routes: Routes = [
  {
    route: '/',
    path: '/pages/index',
    name: 'Page',
  },
  {
    route: '/countdown',
    path: '/pages/countdown/index',
    name: 'Page',
  },
];

export const tabBar = {};
