import { Routes } from '@ray-js/types';

export const routes: Routes = [
  {
    route: '/',
    path: '/pages/index',
    name: 'Page',
  },
  {
    route: '/test',
    path: '/pages/Test/index',
    name: 'Test',
  },
];

export const tabBar = {};
